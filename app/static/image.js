function Image(imageData){
    /**
     * This create a new object, which represents an image and eventual segmentations of the image.
     *
     * @param imageData: the object representation as sent by the server
     */

}